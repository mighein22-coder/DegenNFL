{
  "name": "degennfl-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd src && npm run dev",
    "build": "cd src && npm run build",
    "preview": "cd src && npm run preview",
    "typecheck": "npm --prefix src run typecheck && npm --prefix netlify/functions run typecheck",
    "test": "cd src && npm run test",
    "install_all": "npm --prefix src ci && npm --prefix netlify/functions ci"
  }
}
