# Copy to .env.local and fill in from the DegenNFL Supabase project settings.
# Both are inlined into the public JS bundle by Vite — never put a secret here.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
